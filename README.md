# 📚 Руководство по моей конфигурации NixOS

## 🏗️ **Обновленная структура проекта**

```
/etc/nixos/                    ← Главная папка с настройками
├── 📄 configuration.nix       ← ГЛАВНЫЙ ФАЙЛ - "оглавление" системы
├── 📄 hardware.nix            ← Настройки железа (процессор, экран и т.д.)
├── 📦 assets/                 ← Ресурсы системы (обои, шрифты, видео)
│   ├── 🖼️ images/            ← Папка со статичными обоями (JPG, PNG)
│   ├── 📽️ videos/            ← Папка с видео-обоями (MP4, WebM, GIF)
│   ├── 🅰️ fonts/             ← Пользовательские шрифты
│   ├── 📜 wallpaper-manager.sh   ← Скрипт для статичных обоев
│   └── 📽️ video-wallpaper.sh     ← Скрипт для видео-обоев
├── 📦 home/                   ← Личные настройки пользователя
│   ├── 📄 home.nix            ← Конфигурация Home Manager
│   ├── 📄 firefox.nix         ← Настройки браузера Firefox
│   ├── 📄 desktop.nix         ← Рабочий стол (Hyprland + обои + шрифты)
│   ├── 📄 programs.nix        ← Пользовательские программы
│   │   ├── OBS Studio         ← Запись экрана и стриминг
│   │   ├── Obsidian           ← Заметки и организация
│   │   ├── Telegram           ← Мессенджер
│   │   └── Discord            ← Голосовой чат
│   └── 📄 vpn.nix             ← Настройки AmneziaVPN
└── 📦 modules/                ← Системные модули
    ├── 📄 network.nix         ← Интернет и сетевые настройки
    ├── 📄 security.nix        ← Безопасность и фаервол
    └── 📄 packages.nix        ← Системные пакеты
```

## 🚀 **Быстрый старт**

### **Базовые команды NixOS**

```bash
# Проверить настройки на ошибки
sudo nixos-rebuild dry-build

# Применить настройки БЕЗ перезагрузки (тест)
sudo nixos-rebuild test

# Применить настройки навсегда
sudo nixos-rebuild switch

# Откатиться к предыдущей конфигурации
sudo nixos-rebuild switch --rollback

# Обновить все программы
sudo nix-channel --update && sudo nixos-rebuild switch
```

### **Инициализация системы**

```bash
# Создать папки для ресурсов
sudo mkdir -p /etc/nixos/assets/{images,videos,fonts}

# Сделать скрипты исполняемыми
sudo chmod +x /etc/nixos/assets/wallpaper-manager.sh
sudo chmod +x /etc/nixos/assets/video-wallpaper.sh

# Применить конфигурацию
sudo nixos-rebuild switch
```

## 🎨 **Управление обоями (статика + видео)**

### **Статические обои**

```bash
# Добавить новые обои
cp ~/Загрузки/мои-обои.jpg /etc/nixos/assets/images/

# Установить случайные обои
/etc/nixos/assets/wallpaper-manager.sh random

# Включить автосмену каждые 30 минут
/etc/nixos/assets/wallpaper-manager.sh auto

# Меню управления
/etc/nixos/assets/wallpaper-manager.sh
```

### **Видео-обои (MP4)**

```bash
# Добавить видео-обои
cp ~/Загрузки/видео-обои.mp4 /etc/nixos/assets/videos/

# Оптимизировать видео для обоев (рекомендуется)
video-wallpaper optimize /etc/nixos/assets/videos/видео-обои.mp4

# Запустить случайные видео-обои
video-wallpaper random

# Остановить видео-обои
video-wallpaper stop

# Меню управления видео-обоями
video-wallpaper
```

### **Автоматическая смена**
- **Статические обои:** каждые 30 минут (можно изменить в `desktop.nix`)
- **Видео-обои:** каждые 1 час (можно изменить в `desktop.nix`)
- **Чередование:** можно настроить в `desktop.nix` в разделе `exec-once`

## 🖥️ **Рабочий стол Hyprland**

### **Основные горячие клавиши**

| Комбинация | Действие |
|------------|----------|
| `Win + Q` | Открыть терминал (Kitty) |
| `Win + C` | Закрыть активное окно |
| `Win + D` | Меню программ (Rofi) |
| `Win + W` | Открыть Firefox |
| `Win + 1-9` | Переключить рабочий стол |
| `Win + Shift + 1-9` | Переместить окно |

### **Управление обоями**

| Комбинация | Действие |
|------------|----------|
| `Win + Shift + W` | Случайные статические обои |
| `Win + Alt + W` | Меню статических обоев |
| `Win + Ctrl + V` | Случайные видео-обои |
| `Win + Ctrl + Shift + V` | Остановить видео-обои |
| `Win + Ctrl + Alt + V` | Меню видео-обоев |

### **Быстрый доступ к программам**

| Комбинация | Действие |
|------------|----------|
| `Win + Shift + T` | Telegram |
| `Win + Shift + D` | Discord |
| `Win + Shift + O` | OBS Studio |
| `Win + Shift + N` | Obsidian |
| `Win + Ctrl + Shift + S` | Начать запись в OBS |
| `Win + Ctrl + Shift + X` | Остановить запись в OBS |

## 📦 **Установленные программы**

### **Основные приложения:**
- **Firefox** - браузер с приватными настройками
- **OBS Studio** - запись экрана и стриминг
- **Obsidian** - система заметок и организации знаний
- **Telegram Desktop** - мессенджер
- **Discord** - голосовой чат для гейминга и общения
- **VLC** - видеоплеер
- **LibreOffice** - офисный пакет
- **GIMP** - редактор изображений

### **Утилиты:**
- **Kitty** - современный терминал
- **Waybar** - панель состояния
- **Rofi** - меню запуска программ
- **Dunst** - система уведомлений
- **Flameshot** - скриншоты
- **GParted** - управление дисками

## 🎬 **OBS Studio - настройка для Honor MagicBook 14 Pro**

### **Оптимальные настройки:**
```bash
# Автоматические настройки уже включены:
- Разрешение записи: 1920x1080 @ 60 FPS
- Битрейт видео: 5000 Kbps
- Битрейт аудио: 160 Kbps
- Формат: MP4
- Папка записей: ~/Videos/OBS Recordings

# Ручная настройка для лучшего качества:
1. Откройте OBS: Win + Shift + O
2. Перейдите в Настройки -> Видео
3. Установите:
   - Базовое разрешение: 3120x2080
   - Выходное разрешение: 1920x1080
   - FPS: 60
4. Выход -> Простой режим -> Качество записи: Высокое
```

### **Горячие клавиши OBS:**
```bash
# Глобальные горячие клавиши (работают даже когда OBS свёрнут):
Win + Ctrl + Shift + S = Начать запись
Win + Ctrl + Shift + X = Остановить запись
```

### **Для экономии ресурсов:**
```bash
# Если запись тормозит:
1. Уменьшите разрешение до 1280x720
2. Уменьшите FPS до 30
3. Закройте ненужные программы
4. Используйте кодировку через GPU (если доступно)
```

## 📓 **Obsidian - система заметок**

### **Быстрый старт:**
```bash
# Папка для заметок уже создана:
~/Documents/Obsidian/

# Структура папок:
- Daily/          # Ежедневные заметки
- Projects/       # Проекты
- References/     # Справочные материалы
- Templates/      # Шаблоны
- Archive/        # Архив

# Создать новую заметку:
echo "# Новая заметка" > ~/Documents/Obsidian/Daily/$(date '+%Y-%m-%d').md
```

### **Рекомендуемые плагины:**
1. **Calendar** - календарь для ежедневных заметок
2. **Dataview** - запросы к заметкам как к базе данных
3. **Templater** - шаблоны для заметок
4. **QuickAdd** - быстрая запись идей
5. **Excalidraw** - рисование диаграмм

## 💬 **Telegram и Discord**

### **Telegram Desktop:**
- **Горячая клавиша:** `Win + Shift + T`
- **Секретные чаты:** включены для приватности
- **Темная тема:** автоматически настроена
- **Уведомления:** настроены через Dunst

### **Discord:**
- **Горячая клавиша:** `Win + Shift + D`
- **Push-to-talk:** рекомендуется для голосовых каналов
- **Темная тема:** автоматически включена
- **Аппаратное ускорение:** отключено для стабильности

## 🔐 **AmneziaVPN**

### **Установка и запуск:**
```bash
# 1. Скачать AppImage с https://amnezia.org/download
# 2. Поместить в ~/Downloads/AmneziaVPN.AppImage

# Запуск:
amnezia-vpn

# Или вручную:
chmod +x ~/Downloads/AmneziaVPN.AppImage
~/Downloads/AmneziaVPN.AppImage
```

### **Проверка VPN:**
```bash
# Скрипт для проверки
check-vpn

# Или вручную:
curl ifconfig.me                      # Проверить IP
dig +short myip.opendns.com @resolver1.opendns.com  # Проверить DNS утечки
```

## 🖥️ **Оптимизация для Honor MagicBook 14 Pro**

### **Автоматические настройки:**
- **HiDPI масштабирование:** 1.5x для экрана 3120x2080
- **Управление питанием:** TLP для экономии батареи
- **Звук:** PipeWire для современного аудио
- **Тачпад:** жесты и тап-клик
- **Яркость:** работают клавиши F1/F2

### **Ручная оптимизация:**
```bash
# Проверить температуру
sensors

# Мониторинг ресурсов
htop

# Управление яркостью
brightnessctl s 50%  # Установить 50% яркость
```

## 🔧 **Частые задачи**

### **Добавить новую программу:**
1. Найти пакет: `nix search nixpkgs [имя]`
2. Добавить в `/etc/nixos/home/programs.nix`
3. Выполнить: `sudo nixos-rebuild switch`

### **Изменить настройки Firefox:**
1. Отредактировать `/etc/nixos/home/firefox.nix`
2. Добавить расширения: `pkgs.nur.repos.rycee.firefox-addons`
3. Выполнить: `sudo nixos-rebuild switch`

### **Добавить новые шрифты:**
```bash
# 1. Скопировать .ttf/.otf файлы в /etc/nixos/assets/fonts/
# 2. Обновить кэш шрифтов:
fc-cache -fv
```

## ⚠️ **Решение проблем**

### **Система не загружается:**
```bash
# Выберите предыдущую версию в меню загрузки
# Или если вошли в систему:
sudo nixos-rebuild switch --rollback
```

### **Ошибки в конфигурации:**
```bash
# Проверить синтаксис
sudo nix-instantiate --parse /etc/nixos/configuration.nix

# Посмотреть что изменится
sudo nixos-rebuild dry-build
```

### **Пакет не найден:**
```bash
# Проверить существует ли пакет
nix search nixpkgs [имя]

# Попробовать unstable канал
sudo nix-channel --add https://nixos.org/channels/nixos-unstable nixos-unstable
sudo nix-channel --update
sudo nixos-rebuild switch
```

### **Видео-обои не работают:**
```bash
# Проверить MPV
mpv --version

# Проверить что видео файлы есть
ls /etc/nixos/assets/videos/

# Проверить логи
cat ~/.cache/video-wallpaper.log
```

## 🔄 **Лучшие практики**

### **Работа с Git:**
```bash
cd /etc/nixos
sudo git init
sudo git add .
sudo git commit -m "Начальная конфигурация"

# После изменений:
sudo git add .
sudo git commit -m "Описание изменений"
```

### **Резервное копирование:**
```bash
# Перед изменениями
sudo cp /etc/nixos/configuration.nix /etc/nixos/configuration.bak.nix

# Экспорт всей конфигурации
sudo tar -czf nixos-backup-$(date +%Y%m%d).tar.gz /etc/nixos/
```

### **Регулярное обслуживание:**
```bash
# Раз в месяц - обновление
sudo nix-channel --update
sudo nixos-rebuild switch --upgrade

# Очистка старых поколений
sudo nix-collect-garbage -d

# Проверка диска
sudo df -h
sudo du -sh /nix/store/
```

## 📋 **Чек-лист после установки**

### **OBS Studio:**
- [ ] Проверить запись экрана (тест 10 секунд)
- [ ] Проверить запись микрофона
- [ ] Настроить горячие клавиши записи
- [ ] Выбрать папку для сохранения записей

### **Obsidian:**
- [ ] Открыть Obsidian (`Win + Shift + N`)
- [ ] Создать первую заметку
- [ ] Установить полезные плагины
- [ ] Настроить синхронизацию (опционально)

### **Telegram/Discord:**
- [ ] Войти в аккаунты
- [ ] Настроить уведомления
- [ ] Включить темную тему
- [ ] Настроить качество звонков

### **VPN:**
- [ ] Скачать AmneziaVPN AppImage
- [ ] Протестировать подключение
- [ ] Проверить отсутствие утечек

### **Обои:**
- [ ] Добавить свои обои в `assets/images/`
- [ ] Добавить видео-обои в `assets/videos/`
- [ ] Протестировать автоматическую смену

## 🛠️ **Полезные команды**

### **Диагностика:**
```bash
# Информация о системе
neofetch

# Температура и вентиляторы
sensors

# Использование диска
df -h

# Использование памяти
free -h

# Загрузка сети
iftop

# Активные процессы
htop
```

### **Управление сервисами:**
```bash
# Автосмена обоев
systemctl --user status auto-wallpaper-timer
systemctl --user enable --now auto-wallpaper-timer
systemctl --user disable auto-wallpaper-timer

# Автосмена видео-обоев
systemctl --user status auto-video-wallpaper.timer
```

## 📚 **Ресурсы**

### **Официальные:**
- **NixOS Wiki:** https://nixos.wiki
- **Поиск пакетов:** https://search.nixos.org/packages
- **Поиск опций:** https://search.nixos.org/options
- **Документация:** https://nixos.org/learn

### **Сообщества:**
- **Discourse:** https://discourse.nixos.org
- **Reddit:** r/NixOS
- **Matrix:** #nixos:nixos.org
- **Telegram RU:** "NixOS RU"

### **Программы:**
- **OBS Studio:** https://obsproject.com
- **Obsidian:** https://obsidian.md
- **AmneziaVPN:** https://amnezia.org
- **Hyprland:** https://hyprland.org

## 💡 **Советы для новичков**

1. **Начинайте с малого** - не пытайтесь настроить всё сразу
2. **Комментируйте код** - пишите пояснения на русском
3. **Тестируйте перед применением** - используйте `dry-build` и `test`
4. **Используйте Git** - это ваша страховка от ошибок
5. **Сообщество поможет** - не бойтесь задавать вопросы
6. **Документируйте изменения** - ведите changelog
7. **Резервные копии** - регулярно делайте бэкапы важных данных

 В NixOS все изменения делаются через конфигурационные файлы. После редактирования всегда запускайте:
```bash
sudo nixos-rebuild switch
```
